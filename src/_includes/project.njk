---
layout: base.njk
faviconURL: ../../assets/favicon/favicon.ico
styleURL: ../../style.css
extraStyle: ../project.css
---

<div class="post">
    <a href="../../" style="margin-bottom: 1rem;">🏡 home</a>
    <div class="title">
        {{ projectTitle }}
    </div>

    <div class="tags">
        <i class="fas fa-tags"></i>
        {% for tag in tags %}
            <a class="tag" href="{{'/tags/' + tag | url}}">{{ tag }}</a>
        {% endfor %}
    </div>

    {% if headerImg %}
        <div class="headerImg">
            <img src="{{ headerImg }}">
        </div>
    {% endif %}

    <div>
        {{ content | safe }}
    </div>
</div>

{% set previousProject = collections.project | getPreviousCollectionItem(page) %}
{% set nextProject = collections.project | getNextCollectionItem(page) %}
<div class="footer">
    {% if previousProject.url %}
        <a class="prevProj" href="{{ previousProject.url | url }}">
            <i class="fas fa-long-arrow-alt-left"></i>
            prev project
        </a>
    {% else %}
        <a class="prevProj" href="{{ collections.project[collections.project.length - 1].url | url }}">
            <i class="fas fa-long-arrow-alt-left"></i>
            prev project
        </a>
    {% endif %}

    <h2>💻 more stuff! 💻</h2>

    {% if nextProject.url %}
        <a class="nextProj" href="{{ nextProject.url }} ">next project
            <i class="fas fa-long-arrow-alt-right"></i>
        </a>
    {% else %}
        <a class="nextProj" href="{{ collections.project[0].url }}">next project
            <i class="fas fa-long-arrow-alt-right"></i>
        </a>
    {% endif %}
</div>